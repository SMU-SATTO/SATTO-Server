CREATE DATABASE smus;

USE smus;

CREATE TABLE school_notice (
    id INT AUTO_INCREMENT PRIMARY KEY,
    title VARCHAR(255),
    postId INT,
    createdTime DATETIME,
    views INT
);
ALTER TABLE school_notice ADD COLUMN link VARCHAR(255); //링크 데이터 추가

USE smus;
SELECT * FROM school_notice;